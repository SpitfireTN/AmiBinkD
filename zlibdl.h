/*
 *  zlibdl.h -- zlib dynamic load interface (stub)
 *
 *  zlib/bzip2 compression support is not built into this Amiga port
 *  (WITH_ZLIB/WITH_BZLIB2 are not defined) - compress.c's real zlibdl.h
 *  content only matters when those are defined, so this is an
 *  intentionally empty placeholder to satisfy the #include.
 */

#ifndef _ZLIBDL_H_
#define _ZLIBDL_H_
#endif
